# Procedural Conlang Generator
This Python and HTML file makes the creation of fictional or constructed languages (ConLangs) easier through a simple yet versatile UI, completely customizable weighted vowels and consonants, tones, world logic, class system, derivation rules, and IPA configuration.

## Running
Before running the .py, install the requirements:
```
pip install flask flask-session flask-wtf
```
